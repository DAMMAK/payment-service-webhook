apiVersion: v1
kind: Service
metadata:
  name: kafka-service
  namespace: default  # or your preferred namespace
spec:
  selector:
    app: kafka
  ports:
    - name: kafka
      port: 9092
      targetPort: 9092
  type: ClusterIP